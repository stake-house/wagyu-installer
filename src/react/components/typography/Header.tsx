import styled from 'styled-components';
import { MediumBlue } from '../../colors';

export const Header = styled.div`
  font-weight: 700;
  font-size: 35;
  color: ${MediumBlue};
  max-width: 550;
  margin-top: 50;
`;
